// Day 7

const followers = 15000;
const isTechNiche = true;
const hasBadReputation = false;

if (followers > 10000 && isTechNiche === true && hasBadReputation === false) {
    console.log("The influencer is a good fit! Sending a collaboration offer.");
} else {
    console.log("The influencer does not meet the campaign requirements.");
}
